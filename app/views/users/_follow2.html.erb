<%= form_for(current_user.following_relationships.build(following_id: user.id), url: user_relationships_path(user)) do |f| %>
	<div><%= f.hidden_field :following_id %></div>
	<%= f.submit "フォローする", class: 'btn btn-link' %>
<% end %>