<%= form_for(current_user.following_relationships.find_by(following_id: user.id), html: { method: :delete }, url: user_relationships_path(user)) do |f| %>
	<%= hidden_field_tag :following_id, user.id %>
	<%= f.submit "フォロー外す", class: 'btn btn-primary btn-block' %>
<% end %>